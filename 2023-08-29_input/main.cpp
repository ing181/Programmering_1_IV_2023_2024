#include <iostream>

using namespace std;

int main()
{
    // Deklarerar (skapar) en variabel
    // typ namn
    int heltal;
    // inmatningsoperatorn >> tangentbordet(cin)
    cin >> heltal;
    cout << (heltal +1) << endl;
    return 0;
}
